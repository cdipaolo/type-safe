
    def train(self):
        """ trains the weight vector on the training data
        """
        alpha = self._alpha
        
        # going through the training data _maxIter times
        for i in range(_maxIter):
        
            # moving through the training data
            for k in range(self._trainingData):
                self._weight
        